let bgColor;
let sliderBg;

function setup() {
    bgColor = 200;

    // create canvas
    createCanvas(800, 600);
    background(bgColor);

    sliderBg = createSlider(0, 255);
}

function draw() {
    // now students can tell me
    // teacher you forget erase
    background(bgColor);

}