let bgColor;

function setup() {
    bgColor = 200;

    // create canvas
    createCanvas(800, 600);
    background(bgColor);

    createSlider()
}

function draw() {
    // now students can tell me
    // teacher you forget erase
    background(bgColor);

}