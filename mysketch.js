// global variables

// preload
// synchronous wait till all loaded
function preload() {
    // add media files here
}

function setup() {
    createCanvas(600,400);
    background(220);
}

function draw() {
    // circle(50,50,25);
    circle(50,50,25);
}